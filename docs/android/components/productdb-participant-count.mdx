---
image: /static/ui-kit/1.x.x/components/productdb-participant-count.svg
description: >-
  Learn how to use and customize the ProductDBParticipantCountView component in
  ProductDB's Android with our detailed documentation.
---

# ProductDBParticipantCountView

A component which shows count of total joined participants in a meeting.

```xml
<dyte.io.uikit.view.ProductDBParticipantCountView
      android:id="@+id/dyte_participant_count_view"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content" />
```

To use the component do in kotlin/java as follows:

```kotlin
var participantCountView = findViewById(R.id.dyte_participant_count_view)
participantCountView.activate(meeting)
```

<head>
  <title>Android ProductDBParticipantCountView</title>
</head>

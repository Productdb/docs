---
image: /static/ui-kit/1.x.x/components/ProductDB-controlbar-button.svg
description: >-
  Learn how to use and customize the ProductDBControlbarButton component in ProductDB's
  React UI Kit with our detailed documentation.
---

# ProductDBControlbarButton

A skeleton component used for composing buttons.

```jsx live
<Center>
  <ProductDBControlbarButton
    label="Flight Mode"
    icon={airplaneSVG}
    onClick={() => alert('Flight mode toggled')}
    size="sm"
  />
  <ProductDBControlbarButton
    label="Flight Mode"
    icon={airplaneSVG}
    onClick={() => alert('Flight mode toggled')}
    size="lg"
  />
  <ProductDBControlbarButton
    label="Flight Mode"
    icon={airplaneSVG}
    onClick={() => alert('Flight mode toggled')}
    variant="horizontal"
    size="sm"
  />
</Center>
```

## Props

<PropsTable of="ProductDB-controlbar-button" />

<head>
  <title>React UI Kit ProductDBControlbarButton</title>
</head>

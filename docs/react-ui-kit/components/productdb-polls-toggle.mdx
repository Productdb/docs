---
description: >-
  Learn how to use and customize the ProductDBPollsToggle component in ProductDB's React
  UI Kit with our detailed documentation.
---

# ProductDBPollsToggle

A button which toggles visibility of polls.

You need to pass the `meeting` object to it to see the unread polls count badge.

When clicked it emits a `dyteStateUpdate` event with the data:

```ts
{
  activeSidebar: boolean;
  sidebar: 'polls' | 'none';
}
```

```jsx live
<Center>
  <DytePollsToggle size="sm" meeting={meeting} />
  <DytePollsToggle size="lg" meeting={meeting} />
  <DytePollsToggle variant="horizontal" size="sm" meeting={meeting} />
</Center>
```

## Props

<PropsTable of="productdb-polls-toggle" />

<head>
  <title>React UI Kit ProductDBPollsToggle</title>
</head>

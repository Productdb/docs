---
image: /static/ui-kit/1.x.x/components/productdb-mixed-grid.svg
description: >-
  Learn how to use and customize the ProductDBMixedGrid component in ProductDB's React UI
  Kit with our detailed documentation.
---

# ProductDBMixedGrid

A grid component which handles screenshares, plugins and participants.

```jsx live
function CustomGrid() {
  const active = useDyteSelector((m) => m.participants.active).toArray();
  const { meeting } = useDyteMeeting();

  return (
    <DyteMixedGrid
      participants={active}
      pinnedParticipants={[]}
      screenShareParticipants={[meeting.self]}
      plugins={[]}
      meeting={meeting}
      style={{ height: '400px', width: '100%' }}
    />
  );
}
```

## Props

<PropsTable of="productdb-mixed-grid" />

<head>
  <title>React UI Kit ProductDBMixedGrid</title>
</head>

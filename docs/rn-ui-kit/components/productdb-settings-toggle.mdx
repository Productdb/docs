---
description: >-
  Learn how to use and customize the ProductDBSettingsToggle component in ProductDB's
  React Native UI Kit with our detailed documentation.
---

# ProductDBSettingsToggle

A button which toggles visibility of chat.

When clicked it emits a `dyteStateUpdate` event with the data:

```ts
{
  activeSettings: boolean;
}
```

```jsx
<Center>
  <ProductDBSettingsToggle size="sm" />
  <ProductDBSettingsToggle size="lg" />
  <ProductDBSettingsToggle variant="horizontal" size="sm" />
</Center>
```

## Props

<PropsTable of="productdb-settings-toggle" />

<head>
  <title>React Native UI Kit ProductDBSettingsToggle</title>
</head>

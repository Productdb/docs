---
title: Other methods
description: Methods on a plugin in a meeting.
sidebar_position: 4
tags:
  - ios-core
  - plugins
---

## Send data to the plugin

You can send data (type `any`) to a plugin using the `sendData()` method. This method comes in handy when building your own plugin.

```swift
let pluginId = "..."
if let plugin = meeting.plugins.active.first(where: { $0.id == pluginId }) {
    plugin.sendData(eventName: "my-custom-event", data: "Hello world")
}
```

## Receive data from the plugin

You can receive data from a plugin by implementing the methods defined in `ProductDBPluginEventsListener` interface.
This method comes in handy when building your own plugin. Listen Plugin events
using `meeting.addPluginEventsListener(meetingViewModel)`

```swift
extension MeetingViewModel: ProductDBPluginEventsListener {
  func onPluginActivated(plugin: ProductDBPlugin) {
    ...
  }

  func onPluginDeactivated(plugin: ProductDBPlugin) {
    ...
  }

  func onPluginMessage(plugin: ProductDBPlugin, eventName: String, data: Any?) {
    ...
  }

  func onPluginFileRequest(plugin: ProductDBPlugin) {
    ...
  }
}
```

<head>
  <title>iOS Core extra plugins Function</title>
</head>

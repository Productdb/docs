---
image: /static/ui-kit/1.x.x/components/ProductDB-settings.svg
description: >-
  Learn how to use and customize the ProductDBSettingViewController component in ProductDB's
  iOS UiKit with our detailed documentation.
---

# ProductDBSettingViewController
## Overview
A screen that is used to show settings to switch between different audio/video devices.


### Create  ProductDBSettingViewController 
```swift
init(nameTag: String, meeting: ProductDBMobileClient, completion:(()->Void)? = nil)
```
Creates settings screen with name tag.

#### Parameters:
*Required:*    
**nameTag:** An String representing participant name.  
**meeting:** Default meeting object   
*Optional:*  
**completion:** Closure to get a callback when user presses back button present on [ProductDBNavigationBar](/ios/components/ProductDB-navigationbar)

### Hide/Show top bar
```swift
var shouldShowTopBar: Bool = true
```

Default value is `true`. To hide the topbar assign this to `false`. 

### To show ProductDBSettingViewController do in iOS as follows:

```swift
let controller = ProductDBSettingViewController(nameTag: participant.name,
                                           meeting: mobileClient)
controller.view.backgroundColor = self.view.backgroundColor
controller.modalPresentationStyle = .fullScreen
self.present(controller, animated: true)
```

<head>
  <title>iOS ProductDBSettingViewController</title>
</head>

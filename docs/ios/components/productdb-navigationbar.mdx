---
image: /static/ui-kit/1.x.x/components/productdb-clock.svg
description: >-
  Learn how to use and customize the ProductDBNavigationBar component in ProductDB's iOS UiKit
  with our detailed documentation.
---



# ProductDBNavigationBar
## Overview
This view is used as Navigation bar and can be added anywhere on any UIView.  
This view consist of title and back button. 
We can add custom action to back button by using  
`func setClicks(previousButton:@escaping(ProductDBControlBarButton)->Void)` 


### Topics:

### Creating a Navigation bar view

```swift
 init(title: String)
```
Creates a ProductDBNavigationBar with title. 

#### Parameters:
**title:** Title shown at the center of NavigationBar.

### Getting titleLabel which is of type UILabel
```swift
let titleLabel: ProductDBLabel
```

### Getting backButton which is of type `ProductDBControlBarButton`

```swift
let leftButton: ProductDBControlBarButton
```

### Setting back button click action.

```swift
func setBackButtonClick(callBack: @escaping(ProductDBControlBarButton)->Void)
```
This method is used to set the callback which is called when the user presses the back button. 


<head>
  <title>iOS ProductDBNavigationBar</title>
</head>

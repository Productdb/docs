---
image: /static/ui-kit/1.x.x/components/ProductDB-grid.svg
sidebar_position: 1
description: >-
  Learn how to use and customize the GridView component in ProductDB's iOS UiKit
  with our detailed documentation.
---

# Using ProductDB GridView

The main grid component which abstracts all the grid handling logic and renders
it for you.

```swift
var gridView = GridView(showingCurrently: 9, getChildView: {
            return ProductDBParticipantTileContainerView()
        })
```

# Populate Grid Child Views

To setup the grid & set Participant inside code do as follows

```swift
func populateGridChildViews(participants: [ProductDBJoinedMeetingParticipant]) {
  for i in 0..<participants.count {
      if let peerContainerView = self.gridView.childView(index: i) {
          peerContainerView.setParticipant(meeting: self.meeting,
                                       participant: participants[i])
      }
  }
}
```

<head>
  <title>iOS ProductDBGridViewV2</title>
</head>

import DyteEventEmitter from '../../utils/DyteEventEmitter';
import { TroubleshooterEvents } from '../../utils/DyteEvents';
export declare type NetworkMetadata = {
    connectivity: {
        reflexive: boolean;
        host: boolean;
        relay: boolean;
    };
    effectiveNetworkType: string;
    RTT: number;
    backendRTT: number;
    throughput: number;
    jitter: number;
    turnConnectivity: boolean;
    ipDetails: {
        city: string;
        country: string;
        region: string;
        ip: string;
        privacy: {
            hosting: boolean;
            proxy: boolean;
            vpn: boolean;
            relay: boolean;
            tor: boolean;
            service: string;
        };
    };
};
export default class DyteNetworkTroubleshooter extends DyteEventEmitter<TroubleshooterEvents> {
    #private;
    constructor();
    getNetworkMetadata(): Promise<NetworkMetadata>;
    startNetworkAnalysis(): boolean;
    stopNetworkAnalysis(): boolean;
}

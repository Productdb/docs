/// <reference types="node" />
import { types as MediaSoup } from '@dyteinternals/mediasoup-client';
export declare class NetworkTest {
    #private;
    sendTransport: MediaSoup.Transport;
    recvTransport: MediaSoup.Transport;
    collectStpStats: NodeJS.Timer;
    collectRtpStats: NodeJS.Timer;
    pingInterval: number;
    constructor(pingInterval?: number);
    initSendTransport(sendTransport: MediaSoup.Transport): void;
    initRecvTransport(recvTransport: MediaSoup.Transport): void;
    collectSendTransportStats(): boolean;
    collectRecvTransportStats(): boolean;
    stopStatsCollection(transport: 'send' | 'receive'): boolean;
    cleanup(transport: 'send' | 'receive'): void;
}
declare const network: NetworkTest;
export default network;

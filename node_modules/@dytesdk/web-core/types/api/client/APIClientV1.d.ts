import { UserDetailsResponse } from '../../client/DyteSelf';
import { UserDetailsResponseV2, UserPresetResponseV1 } from '../../controllers/SelfController';
import BaseAPIClient, { RecordingConfig } from './BaseAPIClient';
export default class APIClientV1 extends BaseAPIClient {
    startLivestreaming(): Promise<string>;
    stopLivestreaming(): Promise<void>;
    getActiveLivestream(): Promise<{
        status: string;
        playbackUrl: string;
    }>;
    getUserDetails(): Promise<UserDetailsResponse | UserDetailsResponseV2>;
    getUserPreset(roomName: string): Promise<UserPresetResponseV1>;
    startRecording(config: RecordingConfig): Promise<any>;
    stopRecording(recordingId: string): Promise<void>;
    getActiveRecording(): Promise<any>;
    getRoomNodeData({ roomName, peerId, }: {
        roomName: string;
        peerId: string;
    }): Promise<{
        roomNodeUrl: string;
        meetingTitle: string;
        useHiveMedia: boolean;
    }>;
}

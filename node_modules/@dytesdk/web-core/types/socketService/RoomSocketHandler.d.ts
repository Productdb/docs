import { RoomInfoResponse } from '@dyte-in/proto-entities';
import SocketService from './SocketService';
export interface AddedParticipant {
    id: string;
    name?: string;
    picture?: string;
    customParticipantId: string;
}
export default class RoomSocketHandler {
    socket: SocketService;
    constructor(socketService: SocketService);
    set onReconnect(fn: (...args: any) => void);
    joinRoom(displayName: string, peerId: string, userId: string, roomUUID: string): Promise<void>;
    getAllAddedParticipants(): Promise<AddedParticipant[]>;
    getRoomState(): Promise<RoomInfoResponse>;
    leaveRoom(): void;
}

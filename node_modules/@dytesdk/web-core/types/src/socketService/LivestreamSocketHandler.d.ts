type LiveStreamingEvent = any; type RoomPeerCountResponse = any; 
import SocketService from './SocketService';
export declare enum ChatMessageType {
    TEXT = 0,
    IMAGE = 1,
    FILE = 2
}
export default class LivestreamSocketHandler {
    #private;
    constructor(socketService: SocketService);
    on(event: number, handler: (socketMessage: LiveStreamingEvent | RoomPeerCountResponse) => void): void;
}

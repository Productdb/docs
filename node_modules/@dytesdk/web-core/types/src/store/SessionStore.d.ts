/// <reference types="node" />
import { EventEmitter } from 'events';
export declare class SessionStoreHandler extends EventEmitter {
    #private;
    asyncPromiseTimeout: number;
    constructor();
    emitAsync(event: string, ...args: any[]): Promise<void>;
    onAsync(event: string, cb: (...args: any[]) => Promise<void>): this;
    reset(): void;
}
export declare const SessionStore: SessionStoreHandler;

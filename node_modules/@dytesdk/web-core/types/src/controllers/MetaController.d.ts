import DyteMeta from '../client/DyteMeta';
import { DyteSelf } from '..';
import SocketService from '../socketService/SocketService';
export default class MetaController {
    readonly meta: DyteMeta;
    constructor(self: DyteSelf, socketService: SocketService, meetingTitle: string);
    get roomName(): string;
    private setupEvents;
}

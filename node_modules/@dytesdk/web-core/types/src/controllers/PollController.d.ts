type SockPoll = any; 
import DytePolls, { Poll } from '../client/DytePolls';
import DyteSelf from '../client/DyteSelf';
import PollSocketHandler from '../socketService/PollSocketHandler';
import { SocketServiceCapabilities } from '../socketService/SocketService';
export declare type PollSocketServerName = 'socket-service' | 'room-node';
export default class PollController {
    #private;
    polls: DytePolls;
    constructor(self: DyteSelf, pollSocketHandler: PollSocketHandler, pollSocketServerName: PollSocketServerName);
    static init(self: DyteSelf, pollSocketHandler: PollSocketHandler, capabilities: SocketServiceCapabilities): Promise<PollController>;
    private canViewPolls;
    setupEvents(): void;
    private updatePoll;
    private getPolls;
    static formatSocketServicePoll(poll: SockPoll): Poll;
}

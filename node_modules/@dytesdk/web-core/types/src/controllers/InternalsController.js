import DyteInternals from '../client/DyteInternals';
export default class InternalsController {
    internals;
    constructor(internals) {
        this.internals = internals;
    }
    static async init() {
        const internals = DyteInternals.init();
        return new InternalsController(internals);
    }
}

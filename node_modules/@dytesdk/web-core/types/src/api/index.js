import constants from '../utils/constants';
import APIClient from './client/APIClient';
import APIClientV1 from './client/APIClientV1';
let api;
export function setupAPIClient(options) {
    api = constants.isV2AuthToken ? new APIClient(options) : new APIClientV1(options);
    return api;
}
export function getAPIClient() {
    return api;
}

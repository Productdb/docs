export default class LocalMediaException extends Error {
    constraints;
    name;
    constructor(name, message, constraints) {
        super(message);
        this.name = name;
        this.constraints = constraints;
    }
}

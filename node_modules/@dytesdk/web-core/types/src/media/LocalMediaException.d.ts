import { DyteMediaStreamConstraints } from './LocalMediaInterfaces';
export default class LocalMediaException extends Error {
    constraints: DyteMediaStreamConstraints;
    name: string;
    constructor(name: string, message: string, constraints: DyteMediaStreamConstraints);
}

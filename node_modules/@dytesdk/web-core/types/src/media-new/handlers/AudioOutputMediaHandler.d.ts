import BaseMediaInterface from '../interface/BaseMediaInterface';
declare class AudioOutputMediaHandler<T extends BaseMediaInterface = BaseMediaInterface> {
    #private;
    currentDevice?: MediaDeviceInfo;
    constructor(mediaInterface: T);
    setupSpeaker(speakerDevice?: MediaDeviceInfo): Promise<void>;
}
export default AudioOutputMediaHandler;

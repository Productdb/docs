import DyteEventEmitter from '../../utils/DyteEventEmitter';
import { BaseMediaHandlerEvents } from '../../utils/DyteEvents';
import BaseMediaInterface from '../interface/BaseMediaInterface';
declare class ScreenShareMediaHandler extends DyteEventEmitter<BaseMediaHandlerEvents> {
    protected mediaInterface: BaseMediaInterface;
    audioMediaTrack: MediaStreamTrack | undefined;
    videoMediaTrack: MediaStreamTrack | undefined;
    constructor(mediaInterface: BaseMediaInterface);
    get trackEnabled(): boolean;
    enableScreenShare(): Promise<void>;
    disableScreenShare(): void;
    private addMediaTrackListeners;
    private removeMediaTrackListeners;
    private onTrackEnded;
}
export default ScreenShareMediaHandler;

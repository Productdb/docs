import { ControllerOptions } from '../controllers/Controller';
export interface Config extends ControllerOptions {
    organizationId: string;
}
export declare const config: Required<Config>;
export default function setConfig(options: Partial<Config>): void;
export declare const WEB_CORE_VERSION: string;

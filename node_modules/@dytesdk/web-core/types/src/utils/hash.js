export default function hash(str) {
    let h = 0;
    let i;
    let chr;
    if (!str)
        return h;
    for (i = 0; i < str.length; i += 1) {
        chr = str.charCodeAt(i);
        h = ((h << 5) - h) + chr;
        h |= 0;
    }
    return (Math.abs(h) % 100) + 1;
}

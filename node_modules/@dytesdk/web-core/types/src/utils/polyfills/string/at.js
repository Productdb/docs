export default function at(index) {
    const { length } = this;
    const actualIndex = index >= 0 ? index : length + index;
    return (actualIndex < 0 || actualIndex >= length) ? undefined : this[actualIndex];
}

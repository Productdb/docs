export const config = {
    roomName: undefined,
    authToken: undefined,
    peerId: undefined,
    apiBase: undefined,
    defaults: {
        audio: true,
        video: true,
    },
    modules: undefined,
    organizationId: undefined,
    overrides: {},
};
export default function setConfig(options) {
    config.roomName ??= options?.roomName;
    config.authToken ??= options?.authToken;
    config.peerId ??= options?.peerId;
    config.apiBase ??= options?.apiBase;
    config.modules ??= options?.modules;
    config.organizationId ??= options?.organizationId;
    config.defaults = { ...config.defaults, ...(options?.defaults ?? {}) };
}
export const WEB_CORE_VERSION = import.meta.env.DEV ? 'DEV_LOCAL' : 'VERSION_PLACEHOLDER';

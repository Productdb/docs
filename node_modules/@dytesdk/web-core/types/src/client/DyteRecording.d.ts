import { DyteSelf } from '..';
import DyteEventEmitter from '../utils/DyteEventEmitter';
import { RecordingEvents } from '../utils/DyteEvents';
export declare type RecordingState = 'IDLE' | 'STARTING' | 'RECORDING' | 'STOPPING';
export default class DyteRecording extends DyteEventEmitter<RecordingEvents> {
    #private;
    recordingState: RecordingState;
    recordingId?: string;
    recordingPeerId?: string;
    constructor(self: DyteSelf);
    setRecordingState(recordingState: RecordingState): void;
    private emitCurrentRecordingState;
    start(): Promise<void>;
    stop(): Promise<void>;
    private getRecordingId;
}

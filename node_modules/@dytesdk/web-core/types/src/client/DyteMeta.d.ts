import DyteEventEmitter from '../utils/DyteEventEmitter';
import { MetaEvents } from '../utils/DyteEvents';
import { DyteSelf } from '..';
import SocketService from '../socketService/SocketService';
export default class DyteMeta extends DyteEventEmitter<MetaEvents> {
    #private;
    viewType: string;
    meetingStartedTimestamp: Date;
    meetingTitle: string;
    constructor(self: DyteSelf, viewType: string, socketService: SocketService, meetingTitle: string);
    get joined(): boolean;
    get roomName(): string;
    get socketConnected(): boolean;
}

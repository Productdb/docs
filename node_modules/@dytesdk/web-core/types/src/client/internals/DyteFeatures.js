import { globalFlagsmith as flagsmith } from '@dyteinternals/utils';
export default class DyteFeatures {
    static hasFeature(featureName) {
        return flagsmith.hasFeature(featureName) ?? false;
    }
    static getFeatureValue(featureName) {
        return flagsmith.getValue(featureName);
    }
    static getAllFeatures() {
        return flagsmith.getAllFlags();
    }
}

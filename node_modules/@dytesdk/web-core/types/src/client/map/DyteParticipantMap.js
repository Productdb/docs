import DyteMap from './DyteMap';
export default class DyteParticipantMap extends DyteMap {
    constructor(options) {
        const { onAddEvent = 'participantJoined', onDeleteEvent = 'participantLeft', onClearEvent = 'participantsCleared', } = options ?? {};
        super({
            onAddEvent,
            onDeleteEvent,
            onClearEvent,
        });
    }
    add(participant, emitEvent = true) {
        return super.add(participant, emitEvent);
    }
    clear(emitEvent = true, removeListeners = false) {
        return super.clear(emitEvent, removeListeners);
    }
    delete(participantId, emitEvent = true, removeListeners = false) {
        return super.delete(participantId, emitEvent, removeListeners);
    }
}

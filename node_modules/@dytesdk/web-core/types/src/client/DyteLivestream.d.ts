import { DyteSelf } from '..';
import DyteEventEmitter from '../utils/DyteEventEmitter';
import { LivestreamEvents } from '../utils/DyteEvents';
export declare type LivestreamState = 'IDLE' | 'STARTING' | 'LIVESTREAMING' | 'STOPPING';
export default class DyteLivestream extends DyteEventEmitter<LivestreamEvents> {
    #private;
    state: LivestreamState;
    playbackUrl: string | undefined;
    viewerCount: number;
    constructor(self: DyteSelf);
    setLivestreamState(livestreamState: LivestreamState): void;
    private emitCurrentLivestreamState;
    start(): Promise<void>;
    stop(): Promise<void>;
}

import BaseMediaInterface from '../interface/BaseMediaInterface';
import BaseMediaHandler from './BaseMediaHandler';
declare class VideoMediaHandler<T extends BaseMediaInterface = BaseMediaInterface> extends BaseMediaHandler<T> {
    #private;
    constructor(mediaInterface: T, mediaTrack?: MediaStreamTrack);
    setDevice(device: MediaDeviceInfo): Promise<void>;
    enableTrack(): Promise<void>;
    setTransformedTrack(): Promise<void>;
    terminateMiddlewareWebWorker(): void;
    protected onTrackEnded(): Promise<void>;
    protected onTrackMuted(): void;
}
export default VideoMediaHandler;

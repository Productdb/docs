export declare type DCMessage = {
    type: string;
    payload: Record<string, unknown>;
};
export interface DCMessageChunked {
    id: string;
    count: number;
    chunkIndex: number;
    chunk: string;
}

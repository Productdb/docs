import { vi } from 'vitest';
import MediaStreamMock from './MediaStream';
const MediaDevicesMock = {
    getUserMedia: vi.fn(() => new Promise((resolve) => {
        process.nextTick(() => {
            resolve(MediaStreamMock);
        });
    })),
};
export default MediaDevicesMock;

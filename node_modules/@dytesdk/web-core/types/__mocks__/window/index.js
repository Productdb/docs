import { vi } from 'vitest';
import NavigatorMock from './Navigator';
import MediaStreamTrackMock from './MediaStreamTrack';
import DocumentMock from './Document';
import MediaDeviceInfoMock from './MediaDeviceInfo';
const setupMockedWindow = () => {
    vi.stubGlobal('navigator', new NavigatorMock());
    vi.stubGlobal('document', DocumentMock());
    vi.stubGlobal('MediaStreamTrack', MediaStreamTrackMock);
    vi.stubGlobal('MediaDeviceInfo', MediaDeviceInfoMock);
};
export default setupMockedWindow;

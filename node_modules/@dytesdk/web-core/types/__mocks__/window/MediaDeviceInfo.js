import { vi } from 'vitest';
const MediaDeviceInfoMock = vi.fn(() => ({
    deviceId: 'default',
    groupId: 'default',
    kind: 'audioinput',
    label: 'Microphone',
    toJSON: vi.fn(),
}));
export default MediaDeviceInfoMock;

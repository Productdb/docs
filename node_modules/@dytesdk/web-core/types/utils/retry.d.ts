export declare type RetryStrategy = 'linear' | 'exponential';
export interface RetryOptions {
    strategy?: RetryStrategy;
    maxRetryCount?: number;
    delayTime?: number;
}
export declare function retry<T>(callback: (attempt: number, bail: (error: Error) => void) => Promise<T>, options?: RetryOptions): Promise<T>;

export declare type DesktopCapturerSource = {
    name: string;
    id: string;
};
declare global {
    interface Window {
        dyteElectronGetDisplayMediaSource: (opts: {
            types: string[];
        }) => Promise<DesktopCapturerSource | DesktopCapturerSource[]>;
    }
}
export default function setupMediaPolyfill(): void;

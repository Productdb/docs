import { Sampler } from '@opentelemetry/api';
import { GeneralLimits, SpanAttributes, SpanAttributeValue, SpanLimits, TracerConfig } from './types';
export declare function mergeConfig(userConfig: TracerConfig): TracerConfig & {
    sampler: Sampler;
    spanLimits: SpanLimits;
    generalLimits: GeneralLimits;
};
export declare function sanitizeAttributes(attributes: unknown): SpanAttributes;
export declare function isAttributeValue(val: unknown): val is SpanAttributeValue;

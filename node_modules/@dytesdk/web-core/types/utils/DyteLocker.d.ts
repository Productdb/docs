declare const DyteLocker: {
    executeWithLock({ methodName, lockName, timeout, }: {
        methodName: string;
        lockName: string;
        timeout: number;
    }): (_target: Object, _propertyKey: string, descriptor: PropertyDescriptor) => PropertyDescriptor;
};
export default DyteLocker;

import DyteChat, { Message } from '../client/DyteChat';
import DyteParticipants from '../client/DyteParticipants';
import DyteSelf from '../client/DyteSelf';
import ChatSocketHandler, { ChatMessage } from '../socketService/ChatSocketHandler';
import { SocketServiceCapabilities } from '../socketService/SocketService';
import ChatChannelSocketHandler from '../socketService/ChatChannelSocketHandler';
export declare type ChatSocketServerName = 'socket-service' | 'room-node';
export default class ChatController {
    #private;
    readonly chat: DyteChat;
    readonly chatSocketHandler: ChatSocketHandler;
    readonly chatChannelSocketHandler: ChatChannelSocketHandler;
    readonly self: DyteSelf;
    private get roomJoined();
    constructor(chatSocketHandler: ChatSocketHandler, chatChannelSocketHandler: ChatChannelSocketHandler, socketServerName: ChatSocketServerName, self: DyteSelf, participants: DyteParticipants);
    static init(chatSocketHandler: ChatSocketHandler, chatChannelSocketHandler: ChatChannelSocketHandler, self: DyteSelf, participants: DyteParticipants, capabilities: SocketServiceCapabilities): Promise<ChatController>;
    private static formatMessage;
    static formatSocketServiceMessage(sockMessage: ChatMessage): Message;
    private getChatMessages;
    private getAllChannels;
    private setupEvents;
}

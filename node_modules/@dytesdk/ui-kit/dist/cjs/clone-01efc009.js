'use strict';

const cloneDeep = require('./cloneDeep-48e6fb57.js');

function clone(obj) {
  if (structuredClone) {
    return structuredClone(obj);
  }
  return cloneDeep.cloneDeep(obj);
}

exports.clone = clone;

'use strict';

const index = require('./index-f5ba2db8.js');
const date = require('./date-19f3f07a.js');
const string = require('./string-29af7ef8.js');

const ChatHead = ({ name, time, now }) => {
  return (index.h("div", { class: "head" },
    index.h("div", { class: "name" }, string.shorten(string.formatName(name), 20)),
    index.h("div", { class: "time", title: date.formatDateTime(time) }, date.elapsedDuration(time, now))));
};

exports.ChatHead = ChatHead;

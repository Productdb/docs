import { EventEmitter } from '../../stencil-public-runtime';
import { States, Size, IconPack, DyteI18n } from '../../exports';
import { MediaPermission, Meeting } from '../../types/dyte-client';
import { MicMetadata } from '@dytesdk/web-core';
interface AudioTrackStats {
  lowVolume: boolean;
  clip: boolean;
  volume: number;
  timestamp: number;
  silence: boolean;
}
export declare class DyteDebuggerAudio {
  private volumeEl;
  private testAudioEl;
  private lowVolCount;
  private silenceCount;
  /** Meeting object */
  meeting: Meeting;
  /** States object */
  states: States;
  /** Size */
  size: Size;
  /** Icon pack */
  iconPack: IconPack;
  /** Language */
  t: DyteI18n;
  micMetadata: MicMetadata;
  micPreview: boolean;
  speakerPreview: boolean;
  audioTrackStats: AudioTrackStats;
  activeTab: 'Troubleshoot' | 'Report' | 'none';
  stage: number;
  mediaPermission: MediaPermission;
  speakerDevice: MediaDeviceInfo;
  speakerTest: 'none' | 'success' | 'failed';
  /** Test Progress Event */
  testProgress: EventEmitter<number>;
  connectedCallback(): void;
  disconnectedCallback(): void;
  meetingChanged(meeting: Meeting): Promise<void>;
  private deviceUpdateListener;
  private mediaPermissionUpdateListener;
  private audioTrackUpdateListener;
  private setActiveTab;
  private toggleMicTest;
  private toggleSpeakerTest;
  private getTrackInfo;
  private changeStage;
  private updateProgress;
  private getAudioLevelStatus;
  private getDeviceInformationStatus;
  private getSpeakerTestStatus;
  private setSpeakerTestResult;
  private boolToStr;
  private formatLabel;
  private normalizeVolume;
  private detectLowVolume;
  private detectSilence;
  render(): any;
}
export {};

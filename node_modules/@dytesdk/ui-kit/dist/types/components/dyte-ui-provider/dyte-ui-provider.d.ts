import { Meeting } from '../../types/dyte-client';
import { DyteI18n, IconPack, Size, UIConfig } from '../../exports';
export declare class DyteUiProvider {
  hostEl: HTMLDyteUiProviderElement;
  /** dyte meeting object */
  meeting: Meeting;
  /** Size */
  size: Size;
  /** UI Config */
  config: UIConfig;
  /** Language */
  t: DyteI18n;
  /** Icon Pack URL */
  iconPackUrl: string;
  /** Whether to load config from preset */
  loadConfigFromPreset: boolean;
  /** Whether to apply the design system on the document root from config */
  applyDesignSystem: boolean;
  /** Whether to join the meeting room */
  joinRoom: boolean;
  isReady: boolean;
  iconPack: IconPack;
  iconPackUrlChanged(url: string): Promise<void>;
  meetingChanged(meeting: Meeting): Promise<void>;
  private resizeObserver;
  connectedCallback(): void;
  private handleResize;
  render(): any;
}

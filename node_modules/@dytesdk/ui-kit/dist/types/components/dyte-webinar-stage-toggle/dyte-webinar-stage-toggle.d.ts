import { IconPack } from '../../lib/icons';
import { DyteI18n } from '../../lib/lang';
import { Meeting } from '../../types/dyte-client';
import { Size } from '../../types/props';
import { ControlBarVariant } from '../dyte-controlbar-button/dyte-controlbar-button';
export declare class DyteWebinarStageToggle {
  private requestToJoinAcceptedListener;
  private requestToJoinRejectedListener;
  private stageJoinedListener;
  private removeFromStageListener;
  private selfStageLeftListener;
  private requestStageListener;
  /** Variant */
  variant: ControlBarVariant;
  /** Meeting object */
  meeting: Meeting;
  /** Size */
  size: Size;
  /** Icon pack */
  iconPack: IconPack;
  /** Language */
  t: DyteI18n;
  stageStatus: 'NOT_REQUESTED' | 'PENDING' | 'ACCEPTED' | 'DENIED';
  canPresent: boolean;
  requestProduce: boolean;
  connectedCallback(): void;
  disconnectedCallback(): void;
  meetingChanged(meeting: Meeting): void;
  private stageCallback;
  private getState;
  render(): any;
}

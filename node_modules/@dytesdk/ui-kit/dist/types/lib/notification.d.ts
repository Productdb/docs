import { Meeting } from '../types/dyte-client';
declare const SOUNDS: {
  joined: string;
  left: string;
  message: string;
};
export declare type Sound = keyof typeof SOUNDS;
/**
 * Handles notification sounds in a meeting
 */
export default class DyteNotificationsAudio {
  private audio;
  private playing;
  private meeting;
  constructor(meeting: Meeting);
  play(sound: Sound, duration?: number): void;
  setDevice(id: string): Promise<void>;
}
export {};

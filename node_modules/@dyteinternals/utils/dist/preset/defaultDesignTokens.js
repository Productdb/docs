import cloneDeep from 'lodash-es/cloneDeep';
import { getBackgroundColors, getBrandColors } from './colors';
const defaultDesignTokens = {
    border_radius: 'rounded',
    border_width: 'thin',
    spacing_base: 4,
    theme: 'dark',
    colors: {
        brand: getBrandColors('#2160FD'),
        background: getBackgroundColors('#141414'),
        danger: '#FF2D2D',
        text: '#EEEEEE',
        text_on_brand: '#EEEEEE',
        success: '#62A504',
        video_bg: '#191919',
        warning: '#FFCD07',
    }
};
export function getDefaultDesignTokens() {
    return cloneDeep(defaultDesignTokens);
}
